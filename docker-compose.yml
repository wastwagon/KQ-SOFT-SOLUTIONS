# Default stack: production (Coolify / VPS use this file by default).
# Local development with hot reload:
#   docker compose -f docker-compose.development.yml up
#
# You can still point Coolify at docker-compose.prod.yml explicitly; both resolve
# to the same services.

include:
  - path: docker-compose.prod.yml
