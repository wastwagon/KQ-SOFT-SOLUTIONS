/**
 * Canonical KQ-SOFT brand hex values (app shell + org branding defaults).
 * The API default colours should stay in sync — see `api/src/lib/platformDefaults.ts`.
 * Tailwind scales: `web/src/index.css` (`@theme`) and `web/tailwind.config.js`.
 */
/** Default primary (blue) for UI + report when org has not customised. */
export const BRAND_PRIMARY_HEX = '#0473ea'
/** Default secondary (green) for report accents. */
export const BRAND_SECONDARY_HEX = '#38d200'
