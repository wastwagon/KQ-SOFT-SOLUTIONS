# Production site origin (no trailing slash). Used for canonical URLs, Open Graph, and sitemap.xml.
# VITE_SITE_URL=https://www.your-domain.com

# Optional: app hosted under a subpath (must match Vite `base` and router basename).
# VITE_BASE=/
